---
alwaysApply: true
---

# 🎨 Theming Rules (Dark/Light + Color Variants)

- Use `ThemeProvider` in `app/layout.tsx`
- Class-based dark mode → `<html class="dark">`
- Default to system preference (`prefers-color-scheme`)
- Never hardcode colors → use tokens
- **Advanced theme system** with multiple color variants

## Available Themes
- **Neutral** (default), **Stone**, **Zinc**, **Gray**, **Slate**
- **Red**, **Rose**, **Orange**, **Green**, **Blue**, **Yellow**, **Violet**
- **Dracula** (special dark theme)

## Theme Tokens
| Use | Token |
|------|--------|
| Background | `bg-background` |
| Text | `text-foreground` |
| Card | `bg-card text-card-foreground` |
| Border | `border-border` |
| Muted | `text-muted-foreground` |
| Hover | `hover:bg-accent hover:text-accent-foreground` |
| Focus Ring | `focus-visible:ring-ring` |

**Focus ring:**
```css
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring
```

**Examples**
```tsx
<div className="bg-background text-foreground border-border p-4 rounded-xl">
  <h2 className="text-lg font-semibold">{t('title')}</h2>
  <Button>{t('action')}</Button>
</div>
```
