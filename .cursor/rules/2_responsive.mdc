---
alwaysApply: true
---

# 📱 Responsive Design (Mobile-First)

**Breakpoints** (Tailwind CSS default)
- `sm`: ≥ 640px (small tablets, large phones)
- `md`: ≥ 768px (tablets)
- `lg`: ≥ 1024px (small laptops, desktops)
- `xl`: ≥ 1280px (large desktops)
- `2xl`: ≥ 1536px (extra large screens)

**Authoring Pattern**
- Base layout = mobile (< 640px)
- Scale up progressively at sm/md/lg/xl/2xl
- Use `container mx-auto px-3 sm:px-4 md:px-5 lg:px-6` for responsive padding

**Patterns**
- `grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
- `flex flex-col gap-3 sm:flex-row`
- `next/image` with responsive `sizes`
- `container mx-auto px-3 sm:px-4 md:px-5 lg:px-6` for page layouts
- `py-4 sm:py-5 md:py-6 lg:py-8` for vertical spacing
- `text-sm sm:text-base md:text-lg` for responsive typography
- `space-y-4 sm:space-y-5 md:space-y-6` for responsive spacing

**Example**
```tsx
<section className="container mx-auto px-3 sm:px-4 md:px-5 lg:px-6 py-4 sm:py-5 md:py-6 lg:py-8">
  <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
    ...
  </div>
</section>
```
