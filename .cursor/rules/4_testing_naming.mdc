---
alwaysApply: true
---

# 🧪 Testing, Imports & Naming

## Testing
- Jest + RTL in `src/__tests__`
- Coverage ≥ 80% (configured in jest.config.js)
- Test theme switching, responsive rendering, editor interactions
- Mock network via `src/lib/http/client.ts` or MSW
- **Comprehensive test coverage** for auth flows, forms, and UI components
- **Mock providers** for i18n, theme, and auth contexts

## Imports/Exports
| Type | Convention |
|------|-------------|
| Components | PascalCase |
| Hooks | camelCase |
| Files | kebab-case.tsx |
| Pages/layouts | default export |
| Libs/components | named export |
| Imports | use `@/` alias |
| Size | ≤200 lines/component, ≤300 lines/file |

## File Organization
- **Barrel exports** with `index.ts` files
- **Domain-based** organization (auth/, article/, etc.)
- **Consistent naming** across all modules
- **TypeScript interfaces** in dedicated files
- **Always use `@/` alias; never deep relative paths**

## Performance
- `next/image` with `sizes`: e.g. `sizes="(max-width: 768px) 100vw, (max-width: 1280px) 50vw, 33vw"`
- Defer heavy client components: `dynamic(() => import(...), { ssr: false })`
- Avoid unnecessary hydration; prefer RSC render when possible (public data)
